package dev.bookharbor.app.ui

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/**
 * One size and shape for text fields and the buttons that sit beside or below them, so a form
 * reads as a set: every control is [ControlHeight] tall with [ControlShape] corners.
 */
val ControlHeight = 56.dp
val ControlShape = RoundedCornerShape(14.dp)
