# BookHarbor documentation

- [`architecture.md`](architecture.md): the server and Android modules and the
  seams between them.
- [`api.md`](api.md): the HTTP API under `/api/v1`.
- [`offline-sync.md`](offline-sync.md): how reading progress syncs without ever
  losing a newer position, through client or server outages.
- [`reader.md`](reader.md): EPUB and PDF reading, progress display, and reader
  settings on Android.

Where these documents and the code differ, the tested code is authoritative.
